<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.theoriedesgraphes_synthese">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Arbres"></activity>
        <activity android:name=".Tri_topologique" />
        <activity android:name=".Clique" />
        <activity android:name=".Probleme_sens_unique" />
        <activity android:name=".Coupe_Ensemble_Coupures" />
        <activity android:name=".Arete_coupure" />
        <activity android:name=".Hamiltonien" />
        <activity android:name=".Eulerien" />
        <activity android:name=".Connexity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>